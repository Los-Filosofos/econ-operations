# Public API origin. Leave empty locally to use the development proxy.
# Set an HTTPS backend origin before building for Cloudflare.
VITE_API_BASE_URL=

# Development only; this value is not bundled into the browser.
API_PROXY_TARGET=http://127.0.0.1:8000
